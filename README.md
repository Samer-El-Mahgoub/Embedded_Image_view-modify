# Embedded_Image_view-modify
A C-based tool for dynamic Resource Patching. It injects and replaces embedded BMP images directly inside compiled Windows executables (.exe) using the Win32 API, without needing the source code.
